package modelo.transfers;

/**
 * Tipo enumerado para los dominios de los atributos
 */
public enum TipoDominio {
    BIT,
    BLOB,
    CHAR,
    DATE,
    DATETIME,
    DECIMAL,
    FLOAT,
    GEOMETRY,
    INTEGER,
    TEXT,
    TIME,
    VARCHAR
}
